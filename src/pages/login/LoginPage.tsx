import * as React from 'react';
import LoginComponent from './LoginComponent';

const LoginPage = () => {
    return (
        <div>
            <h2>Welcome Logon</h2>
            <LoginComponent />
        </div>
    );
};

export default LoginPage;